import { ShowOptionButtonBarDirective } from './show-option-button-bar.directive';

describe('ShowOptionButtonBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOptionButtonBarDirective();
    expect(directive).toBeTruthy();
  });
});
